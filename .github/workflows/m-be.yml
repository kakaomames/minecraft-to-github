name: Minecraft Server Engine

on:
  repository_dispatch:
    types: [start_beminecraft]

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      # --- STEP 0: これが最重要！リポジトリのファイルを持ってくる ---
      - name: "Step 0: 📂 リポジトリのチェックアウト"
        uses: actions/checkout@v4

      # --- STEP 1: 環境整備 ---
      - name: "Step 1: 🏗️ 基礎インフラの整備"
        run: |
          echo "==== [STEP 1 START] ===="
          sudo apt-get update && sudo apt-get install -y libcurl4 curl unzip
          echo "Status: OS dependencies installed ✅"
          echo "==== [STEP 1 COMPLETE] ✅ ===="

      # --- STEP 2: カスタムサーバー準備 & 構造構築 ---
      - name: "Step 2: 📦 実行ファイルの奪取 & フォルダ構築"
        run: |
          echo "==== [STEP 2 START] ===="
          echo "Current Path: $(pwd)"
          echo "--- Repository File List ---"
          ls -R
          echo "----------------------------"
          
          mkdir -p bds
          
          # Python工作員によるバイナリ奪取
          FILE_ID="16OlbXH73OzxJvUd80k0ARC0wAzgNm5_-"
          python3 - <<EOF
          import requests, re, os
          session = requests.Session()
          url = f"https://drive.google.com/uc?export=download&id=${FILE_ID}"
          res = session.get(url)
          token_match = re.search(r'name="confirm" value="([^"]+)"', res.text)
          confirm_token = token_match.group(1) if token_match else "t"
          print(f"Token: {confirm_token}")
          final_res = session.get("https://drive.usercontent.google.com/download", params={'id': '${FILE_ID}', 'export': 'download', 'confirm': confirm_token}, stream=True)
          with open("bds/bedrock_server", "wb") as f:
              for chunk in final_res.iter_content(chunk_size=8192): f.write(chunk)
          EOF
          
          chmod +x ./bds/bedrock_server
          
          # リポジトリの config/ と worlds/ を bds/ 内に確実に展開
          echo "Status: Deploying repository configs..."
          if [ -d "./config" ]; then
            cp -rv ./config/* ./bds/
            echo "Status: Config files deployed ✅"
          else
            echo "⚠️ WARNING: 'config' folder not found. Server may fail."
          fi

          if [ -d "./worlds" ]; then
            cp -rv ./worlds ./bds/
            echo "Status: Worlds folder deployed ✅"
          fi
          
          echo "==== [STEP 2 COMPLETE] ✅ ===="

      # --- STEP 3: 通信兵(playit.gg)の配備 ---
      - name: "Step 3: 🌐 playit.ggトンネル兵の配備"
        run: |
          echo "==== [STEP 3 START] ===="
          curl -SsL -o playit https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-linux-amd64
          chmod +x playit
          echo "Status: playit-agent ready ✅"
          echo "==== [STEP 3 COMPLETE] ✅ ===="

      # --- STEP 4: トンネル開通 ---
      - name: "Step 4: ⚡ トンネル開通プロセス"
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
        run: |
          echo "==== [STEP 4 START] ===="
          if [ -z "$PLAYIT_SECRET" ]; then
            ./playit > playit_log.txt 2>&1 &
          else
            ./playit --secret "$PLAYIT_SECRET" > playit_log.txt 2>&1 &
          fi
          sleep 10
          cat playit_log.txt | grep -E "https://playit.gg/claim/|connected" || echo "Status: Waiting..."
          echo "==== [STEP 4 COMPLETE] ✅ ===="

      # --- STEP 5: サーバー最終起動 ---
      - name: "Step 5: 🎮 Minecraft Bedrock Server 起動！"
        working-directory: ./bds
        run: |
          echo "==== [STEP 5 START] ===="
          # 最終確認
          ls -F
          echo "Executing: LD_LIBRARY_PATH=. ./bedrock_server"
          LD_LIBRARY_PATH=. ./bedrock_server
          echo "==== [STEP 5 FINISHED] 🏁 ===="
