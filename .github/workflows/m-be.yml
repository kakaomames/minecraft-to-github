name: Minecraft Server Engine

on:
  repository_dispatch:
    types: [start_beminecraft]

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      # --- STEP 1: 環境整備 ---
      - name: "Step 1: 🏗️ 基礎インフラの整備"
        run: |
          echo "==== [STEP 1 START] ===="
          sudo apt-get update && sudo apt-get install -y libcurl4 curl
          echo "Status: OS dependencies installed ✅"
          echo "==== [STEP 1 COMPLETE] ✅ ===="

# --- STEP 2: カスタムサーバー準備 (パラメータ直撃突破) ---
      - name: "Step 2: 📦 実行ファイルの直接取得（強行突破）"
        run: |
          echo "==== [STEP 2 START] ===="
          FILE_ID="16OlbXH73OzxJvUd80k0ARC0wAzgNm5_-"
          
          # HTMLにあったフォーム内容を元にURLを再構築
          # confirm=t を最初から付与することで警告をバイパスします
          DIRECT_URL="https://drive.google.com/uc?export=download&id=${FILE_ID}&confirm=t"
          echo "DIRECT_URL: ${DIRECT_URL}"
          
          mkdir -p bds
          
          # -L はリダイレクト追跡、-c はクッキー保存（念のため）
          echo "Status: Downloading with confirm=t..."
          curl -L -c /tmp/drive_cookie "${DIRECT_URL}" -o ./bds/bedrock_server
          
          # 実行権限を付与
          chmod +x ./bds/bedrock_server
          
          echo "Status: Download complete ✅"
          
          # 【重要】ファイルがHTMLでないかチェック
          echo "Checking file type (First 10 characters):"
          check_content=$(head -c 10 ./bds/bedrock_server)
          echo "File Content Start: ${check_content}"
          
          if [[ "$check_content" == *"<!"* ]]; then
            echo "❌ ERROR: Still downloaded HTML... Needs cookie-based approach."
            exit 1
          else
            echo "✅ SUCCESS: Binary file detected!"
          fi
          
          ls -lh ./bds/bedrock_server
          echo "==== [STEP 2 COMPLETE] ✅ ===="

      # --- STEP 3: 通信兵(playit.gg)の配備 ---
      - name: "Step 3: 🌐 playit.ggトンネル兵の配備"
        run: |
          echo "==== [STEP 3 START] ===="
          PLAYIT_DL_URL="https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-linux-amd64"
          curl -SsL -o playit ${PLAYIT_DL_URL}
          chmod +x playit
          echo "Status: playit-agent ready ✅"
          echo "==== [STEP 3 COMPLETE] ✅ ===="

      # --- STEP 4: トンネル開通 ---
      - name: "Step 4: ⚡ トンネル開通プロセス"
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
        run: |
          echo "==== [STEP 4 START] ===="
          if [ -z "$PLAYIT_SECRET" ]; then
            ./playit > playit_log.txt 2>&1 &
          else
            ./playit --secret "$PLAYIT_SECRET" > playit_log.txt 2>&1 &
          fi
          sleep 8
          cat playit_log.txt | grep -E "https://playit.gg/claim/|connected" || echo "Status: Waiting..."
          echo "==== [STEP 4 COMPLETE] ✅ ===="

      # --- STEP 5: サーバー最終起動 ---
      - name: "Step 5: 🎮 Minecraft Bedrock Server 起動！"
        run: |
          echo "==== [STEP 5 START] ===="
          cd bds
          # 実行ファイルが直接置かれているので、そのまま叩く！
          echo "Executing: LD_LIBRARY_PATH=. ./bedrock_server"
          LD_LIBRARY_PATH=. ./bedrock_server
          echo "==== [STEP 5 FINISHED] 🏁 ===="
