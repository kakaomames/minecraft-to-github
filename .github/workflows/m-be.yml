name: Minecraft Server Engine

on:
  repository_dispatch:
    types: [start_beminecraft]

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      # --- STEP 0: リポジトリのチェックアウト ---
      - name: "Step 0: 📂 リポジトリのチェックアウト"
        uses: actions/checkout@v4

      # --- STEP 1: 環境整備 ---
      - name: "Step 1: 🏗️ 基礎インフラの整備"
        run: |
          echo "==== [STEP 1 START] ===="
          sudo apt-get update && sudo apt-get install -y libcurl4 curl unzip
          echo "Status: OS dependencies installed ✅"
          echo "==== [STEP 1 COMPLETE] ✅ ===="

      # --- STEP 2: 公式サーバーのセットアップ ---
      - name: "Step 2: 📦 公式最新サーバーの取得"
        run: |
          echo "==== [STEP 2 START] ===="
          # 提供された公式URLを使用
          OFFICIAL_URL="https://www.minecraft.net/bedrockdedicatedserver/bin-linux/bedrock-server-1.21.131.1.zip"
          
          curl -L -A "Mozilla/5.0" -o bds.zip "${OFFICIAL_URL}"
          mkdir -p bds
          unzip bds.zip -d bds
          chmod +x ./bds/bedrock_server
          
          # リポジトリ内の設定とワールドを上書き
          echo "Status: Deploying configs from repository..."
          cp -rv ./config/* ./bds/ 2>/dev/null || true
          cp -rv ./worlds ./bds/ 2>/dev/null || true
          
          echo "==== [STEP 2 COMPLETE] ✅ ===="

      # --- STEP 3: 通信兵(playit.gg)の配備 ---
      - name: "Step 3: 🌐 playit.ggトンネル兵の配備"
        run: |
          echo "==== [STEP 3 START] ===="
          curl -SsL -o playit https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-linux-amd64
          chmod +x playit
          echo "Status: playit-agent ready ✅"
          echo "==== [STEP 3 COMPLETE] ✅ ===="

      # --- STEP 4: トンネル開通 ---
      - name: "Step 4: ⚡ トンネル開通プロセス"
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
        run: |
          echo "==== [STEP 4 START] ===="
          if [ -z "$PLAYIT_SECRET" ]; then
            ./playit > playit_log.txt 2>&1 &
          else
            ./playit --secret "$PLAYIT_SECRET" > playit_log.txt 2>&1 &
          fi
          sleep 12
          cat playit_log.txt | grep -E "https://playit.gg/claim/|connected" || echo "Status: Processing..."
          echo "==== [STEP 4 COMPLETE] ✅ ===="

      # --- STEP 5: サーバー最終起動 ---
      - name: "Step 5: 🎮 カカオサーバー 起動！"
        working-directory: ./bds
        run: |
          echo "==== [STEP 5 START] ===="
          # 最終構成確認
          ls -F
          
          # 公式バイナリで起動！
          echo "Executing: LD_LIBRARY_PATH=. ./bedrock_server"
          LD_LIBRARY_PATH=. ./bedrock_server
          echo "==== [STEP 5 FINISHED] 🏁 ===="
