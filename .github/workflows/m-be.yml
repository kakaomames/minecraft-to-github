name: Minecraft Server Engine

on:
  repository_dispatch:
    types: [start_beminecraft]

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      # --- STEP 1: 環境整備 ---
      - name: "Step 1: 🏗️ 基礎インフラの整備"
        run: |
          echo "==== [STEP 1 START] ===="
          sudo apt-get update && sudo apt-get install -y libcurl4 curl unzip
          echo "Status: OS dependencies installed ✅"
          echo "==== [STEP 1 COMPLETE] ✅ ===="

      # --- STEP 2: カスタムサーバー準備 & 構造構築 ---
      - name: "Step 2: 📦 実行ファイルの奪取 & フォルダ構築"
        run: |
          echo "==== [STEP 2 START] ===="
          # 1. サーバー実行用ディレクトリ作成
          mkdir -p bds
          
          # 2. Python工作員によるバイナリ奪取 (188MB)
          FILE_ID="16OlbXH73OzxJvUd80k0ARC0wAzgNm5_-"
          python3 - <<EOF
          import requests, re, os
          session = requests.Session()
          url = f"https://drive.google.com/uc?export=download&id=${FILE_ID}"
          res = session.get(url)
          token = re.search(r'name="confirm" value="([^"]+)"', res.text).group(1)
          final_res = session.get("https://drive.usercontent.google.com/download", params={'id': '${FILE_ID}', 'export': 'download', 'confirm': token}, stream=True)
          with open("bds/bedrock_server", "wb") as f:
              for chunk in final_res.iter_content(chunk_size=8192): f.write(chunk)
          EOF
          
          # 3. 権限付与
          chmod +x ./bds/bedrock_server
          
          # 4. リポジトリの config/ と worlds/ を bds/ 内に展開
          echo "Status: Deploying repository configs and worlds..."
          cp -r ./config/* ./bds/ 2>/dev/null || echo "No config files found in repo."
          cp -r ./worlds ./bds/ 2>/dev/null || echo "No worlds folder found in repo."
          
          echo "Status: All components assembled in ./bds ✅"
          ls -R ./bds
          echo "==== [STEP 2 COMPLETE] ✅ ===="

      # --- STEP 3: 通信兵(playit.gg)の配備 ---
      - name: "Step 3: 🌐 playit.ggトンネル兵の配備"
        run: |
          echo "==== [STEP 3 START] ===="
          curl -SsL -o playit https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-linux-amd64
          chmod +x playit
          echo "Status: playit-agent ready ✅"
          echo "==== [STEP 3 COMPLETE] ✅ ===="

      # --- STEP 4: トンネル開通 ---
      - name: "Step 4: ⚡ トンネル開通プロセス"
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
        run: |
          echo "==== [STEP 4 START] ===="
          if [ -z "$PLAYIT_SECRET" ]; then
            ./playit > playit_log.txt 2>&1 &
          else
            ./playit --secret "$PLAYIT_SECRET" > playit_log.txt 2>&1 &
          fi
          sleep 10
          cat playit_log.txt | grep -E "https://playit.gg/claim/|connected" || echo "Status: Waiting for connection..."
          echo "==== [STEP 4 COMPLETE] ✅ ===="

      # --- STEP 5: サーバー最終起動 ---
      - name: "Step 5: 🎮 Minecraft Bedrock Server 起動！"
        working-directory: ./bds
        run: |
          echo "==== [STEP 5 START] ===="
          echo "Current Directory: $(pwd)"
          echo "Files in current dir: $(ls)"
          
          # 実行！
          echo "Executing: LD_LIBRARY_PATH=. ./bedrock_server"
          LD_LIBRARY_PATH=. ./bedrock_server
          
          echo "==== [STEP 5 FINISHED] 🏁 ===="
